import {
  StyleSheet
} from 'react-native';

export default StyleSheet.create({
  container: {
    margin: 100,
    alignItems: 'center',
    justifyContent: 'center'
  },

  smallContainer: {
    marginTop: 50,
    alignItems: 'center',
    justifyContent: 'center'
  }
});